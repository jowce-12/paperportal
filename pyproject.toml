[project]
name = "paperportal"
version = "0.1.0"
description = "arXiv LLM / Vision paper list with a local SQLite cache"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "arxiv>=2.1",
    "fastapi>=0.110",
    "requests>=2.31",
    "uvicorn[standard]>=0.29",
]

[tool.uv]
package = false
